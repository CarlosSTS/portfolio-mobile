import styled from "styled-components/native";
import { colors } from "../../common/colors";

export const Container = styled.View`
    flex: 1;
    justify-content: center;
    align-items: center;
    background-color: ${colors.black0verlay};
`;